# SQL

Holds schema and transformation SQL organized by layer.

- `ddl/` for schema and table creation
- `ddl/bronze_weather_api_raw.sql` for the first raw weather table
- `bronze/` for raw-layer SQL helpers
- `silver/` for normalized-layer SQL helpers
- `gold/` for serving-layer SQL helpers
